# URL of the backend API.
# For local development, point to your local Express server:
VITE_BACKEND_API_URL=http://localhost:3001

# For production (Render), replace with your deployed backend URL:
# VITE_BACKEND_API_URL=https://your-backend.onrender.com
